<html>
<head>
	<style>
		body {font-family: sans-serif}
	</style>
</head>
<body>

	<pageamp-dataset :name="list"
	         :server="${makeData()}"
	         :makeData="${function() {
	            var names = pageReadDirectory(pageFSPath);
	            names = stringSort(names);
	            return names.map(function(s) {return {name:s}});
	         }}"/>

	<div :foreach="list:/root/item">$data{@name}</div>

</body>
</html>